# Production environment variables for frontend build
# Set VITE_API_BASE to the public API root for the deployed backend.
# If your frontend is served from the same origin as the API, leave empty.

# Example: https://api.example.com
VITE_API_BASE=

# For Netlify/Vercel deployments, set VITE_DEPLOY_TARGET=netlify to use base="/"
# For Django-served builds, leave unset to use base="/static/"
# VITE_DEPLOY_TARGET=netlify
